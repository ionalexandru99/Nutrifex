import { TabNavigator } from '@core/navigation/TabNavigator';

/**
 * Render the application's tab-based navigation layout.
 *
 * @returns A React element that renders the app's TabNavigator component
 */
export default function TabLayout() {
  return <TabNavigator />;
}
