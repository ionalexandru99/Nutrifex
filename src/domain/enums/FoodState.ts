/**
 * FoodState - Physical state of a food item.
 *
 * Represents the physical form of food, which affects
 * how it's measured, stored, and consumed.
 */
export enum FoodState {
  SOLID = 'SOLID',
  LIQUID = 'LIQUID',
  POWDER = 'POWDER',
  GEL = 'GEL',
  SEMI_SOLID = 'SEMI_SOLID',
}
